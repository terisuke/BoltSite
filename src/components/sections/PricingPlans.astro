---
import SectionTitle from '../ui/SectionTitle.astro';
import Card from '../ui/Card.astro';
import pricingData from '../../data/priceDats.json';
import { loadTranslation, getLocaleFromPathname } from '../../utils/i18n';
import type { PricingPlan } from '../../types';

const { currentPath } = Astro.props;
const locale = getLocaleFromPathname(currentPath || Astro.url.pathname);
const t = await loadTranslation(locale);
const plans: PricingPlan[] = pricingData;
---

<section id="pricing" class="py-20 bg-gray-50">
  <div class="container mx-auto px-4">
    <div class="max-w-6xl mx-auto">
      
      <!-- セクションタイトル -->
      <SectionTitle
        title={t.pricing.title}
        description={t.pricing.subtitle}
      />
      
      <!-- プラン比較カード -->
      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6 mb-12">
        
        <!-- ビギナープラン -->
        <Card
          title={t.pricing.basic.name}
          description={t.pricing.basic.description}
          price={15000}
          features={t.pricing.basic.features}
        />
        
        <!-- スタンダードプラン -->
        <Card
          title={t.pricing.standard.name}
          description={t.pricing.standard.description}
          price={25000}
          features={t.pricing.standard.features}
        />
        
        <!-- プレミアムプラン -->
        <Card
          title={t.pricing.premium.name}
          description={t.pricing.premium.description}
          price={30000}
          features={t.pricing.premium.features}
        />
        
        <!-- エンタープライズプラン -->
        <Card
          title={t.pricing.enterprise.name}
          description={t.pricing.enterprise.description}
          price={50000}
          features={t.pricing.enterprise.features}
        />
      </div>
      
      <!-- 解約ポリシー -->
      <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-6 text-center">
        <div class="flex items-center justify-center gap-2 mb-2">
          <svg class="w-5 h-5 text-yellow-600" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"></path>
          </svg>
          <span class="text-sm font-semibold text-yellow-800">解約ポリシー</span>
        </div>
        <p class="text-sm text-yellow-700">
          1年以内の解約の場合は1年分の料金をお支払いいただきます。<br>
          <span class="font-semibold">長期的なパートナーシップを推奨</span>しております。
        </p>
      </div>
      
      <!-- 共創パートナー優待制度 -->
      <div class="mt-12 bg-gradient-to-r from-primary-50 to-accent-50 rounded-2xl p-8 text-center">
        <h3 class="text-2xl font-bold text-gray-900 mb-4">
          共創パートナー優待制度
        </h3>
        <p class="text-gray-700 mb-6">
          Cor.Incの他のサービスをご利用のお客様には、<br>
          長期的なパートナーシップに基づく特別料金をご提供いたします。
        </p>
        <a href="https://cor-jp.com" target="_blank" class="inline-block bg-primary-500 hover:bg-primary-600 text-white px-8 py-3 rounded-full font-semibold transition-colors duration-300">
          詳細はこちら
        </a>
      </div>
    </div>
  </div>
</section>
